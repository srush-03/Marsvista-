opencv-python>=4.8
numpy>=1.24
PyYAML>=6.0
psutil>=5.9

# Drone communication
dronekit>=2.9
pymavlink>=2.4

# Deep learning (only needed when using CNN detector)
torch>=2.0
torchvision>=0.15

# OAK-D camera support (optional, only if using OAK-D Lite)
depthai>=2.24